spring:
  data:
    mongodb:
      uri: mongodb://localhost:27017/test_db # Hoặc dùng nhúng (embedded)
    redis:
      host: localhost
  rabbitmq:
    addresses: amqp://guest:guest@localhost:5672/ # Giá trị giả để không bị NPE
  mail:
    host: smtp.gmail.com

app:
  jwt:
    secret: "dummy_secret_for_testing_purposes_only_1234567890" # Phải đủ dài
    expiration: 3600000