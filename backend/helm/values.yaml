backend:
  name: backend 
  replicaCount: 3
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxSurge: 2
      maxUnavailable: 1
  image:
    repository: ghcr.io/nguyentukien/pharmagent-backend:latest
    tag: latest
    pullPolicy: Always
  service:
    type: ClusterIP
    port: 8080
  environment:
    COR_ALLOWED_ORIGINS: "*"

database:
  service:
    host: "database-pharmagent-nodeport"
    port: 27017
  environment:
    MONGO_DATABASE: pharmagentdb
    MONGO_USER: n07
    MONGO_PASSWORD: LTW_2026

redis:
  service:
    host: "redis-12088.c325.us-east-1-4.ec2.cloud.redislabs.com"
    port: 12088
  environment:
    REDIS_USER: "default"
    REDIS_PASSWORD: "hM3tdC4ETyAOXeoAtHjwKbHyTYJd6Wfq"

rabbitmq:
  service:
    host: "gerbil.rmq.cloudamqp.com"
    port: 5671
  environment:
    RABBITMQ_USER: "ynkhfetp"
    RABBITMQ_PASSWORD: "iRicAptCctXzrsM4PC9RVBlV-LFlcsW4"
    RABBITMQ_VHOST: "ynkhfetp"

mail:
  environment:
    SPRING_MAIL_USERNAME: "thechalksquad.work@gmail.com"
    SPRING_MAIL_PASSWORD: "vxagswcsettletgb"
    SPRING_MAIL_HOST: "smtp.gmail.com"
    SPRING_MAIL_PORT: "587"

security:
  environment:
    JWT_SECRET: "VGhpc0lzQURldlNlY3JldEtleUZvckpXVFNpZ25pbmdPbmx5ISE="