export function WorkspacePage({ title, description }) {
  return (
    <div className="work-panel">
      <p className="eyebrow">Module placeholder</p>
      <h2>{title}</h2>
      <p>{description}</p>
    </div>
  )
}
